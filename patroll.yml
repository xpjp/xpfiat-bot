########################################################################
# 本来のチャネル以外でコマンドを実行すると警告を発する機能の設定ファイル
########################################################################

# 注意文言
warning_text: "ここでのbalanceはやめてくださいねー"
# 監視対象のコマンド一覧
# Prefixが変更になったときに変更の必要があるのでそのとき考える
target_commands:
#  - ",register"
  - ",balance"
# コマンドを実行しても良いチャネルを登録
# 厳しすぎる場合は禁止チャネルを指定する方向にシフト
# (とはいえ、warningが飛ぶだけで実行できないわけではないのでこれでも良いかもしれない）
allowed_channels:
#  - "experiment" # 開発用
#  - "register-room"
  - "bot-spam"
  - "bot-spam2"
